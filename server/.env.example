# AxoraPay Server Environment Variables

# AI Provider (optional — app works without this in Demo Mode)
GEMINI_API_KEY=

# Server Configuration
PORT=3001
NODE_ENV=development

# Database (auto-created in /data directory)
# DATABASE_URL=./data/axorapay.db
